# 2026 个人 AI 技术栈最小清单

> **核心原则**：在技术通胀的时代，克制是最高的生产力。
> **目标**：不争论框架，不囤积模型，只交付可运行的智能体。

---

## 🛑 负面清单 (Not-To-Do List)

_在做任何加法之前，先确认你**没有**做以下蠢事：_

- [ ] **拒绝模型崇拜**：不碰模型训练，不买显卡，不折腾 CUDA 环境。
- [ ] **拒绝架构过度**：个人项目严禁引入 Kubernetes 和企业级微服务。
- [ ] **拒绝 RAG 滥用**：90% 的场景不需要向量数据库，关键词搜索 + 上下文缓存足矣。
- [ ] **拒绝框架松鼠症**：不学 LangChain 的 100 种用法，只用最基础的工具封装。

---

## ✅ 最小技术栈 (The Minimalist Stack)

_如无必要，勿增实体。这是你的生存套件：_

### 1. 语言与环境

- **Python**：唯一选项。
  - _理由_：AI 的母语。除了 Python，其他都是方言。

### 2. 模型层 (Model as a Service)

_只关注 API 调用，像用水电一样使用智力。_

- **主力 (Smart)**：GPT-4o / Claude 3.5 Sonnet
  - _用途_：复杂逻辑推理、代码生成、长文本分析。
- **副手 (Fast)**：DeepSeek-V3 / GLM-4-Flash
  - _用途_：批量处理、简单分类、高频对话。
- **关键技能**：
  - [ ] **Prompt Engineering**：结构化提示词编写。
  - [ ] **Function Calling**：让模型连接真实世界的唯一桥梁。

### 3. 编排层 (Orchestration)

_这是 2026 年的核心战场。_

- **LangGraph**：唯一推荐。
  - _核心逻辑_：State (状态) + Edge (流转)。
  - _心法_：把“思考过程”画成图，而不是写成链。

### 4. 数据与记忆 (Data & Memory)

_回归朴素，拒绝高大上。_

- **存储**：SQLite / DuckDB / JSON
  - _原则_：能用文件解决的，绝不起数据库服务。
- **工具**：Pandas
  - _用途_：清洗数据，喂给 AI 吃。

---

## 🛠️ 工程规范清单

_像外科医生一样精准管理你的代码。_

- [ ] **目录结构**：
  ```text
  project/
  ├── agents/       # 大脑 (Planner, Executor)
  ├── tools/        # 手脚 (File, SQL, Search)
  ├── graphs/       # 神经 (Main Logic)
  ├── memory/       # 记忆 (JSON/SQLite)
  └── main.py       # 启动
  ```
- [ ] **版本控制**：Git 是底线。每个 Commit 都是思考的快照。
- [ ] **README**：必须写。第一行就要说人话：“这个 Agent 解决了什么具体问题”。

---

## 🚀 立即行动 (Action Plan)

_今晚任务（限时 120 分钟）：_

1.  **新建 Repo**：不要想名字，叫 `my-first-agent` 即可。
2.  **写一句宣言**：在 README 里写下：“本项目旨在把 [你的某个具体经验] 转化为可执行代码。”
3.  **跑通 Hello World**：用 LangGraph 写一个最简单的循环——用户输入 -> AI 思考 -> AI 回复。

**一旦代码跑起来，你就已经领先了 99% 还在囤积教程的人。**
